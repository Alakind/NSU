var x : int;
var y : int;
var a : int;
var b : int;
var z : int;
var t : int;
var s : int;

{
  * {
    { (x > y) ? ; x := (x - y) } U { (x < y) ? ; y := (y - x) }
  };
  (x == y) ?;
  print(x)
}